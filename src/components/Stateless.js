import React from 'react'
const Stateless = ({message}) => {   //message is the argument and after the arrow we have its implementation.
    return (
        <h1>{message}</h1>
    )
}
export default Stateless;